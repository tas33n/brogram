# Headless browser mode:
# true  = run Chrome without UI (lower CPU, best for servers)
# false = show the Chrome window (useful for debugging)
HEADLESS=false

# HTTP port for the built-in status server (/ and /uptime and /health)
PORT=3000

# Directory where your exported Cookie-Editor JSON files live
# The app loads the newest JSON from this folder at startup.
COOKIES_DIR=./cookies

# Which actions (modules) to run at startup, comma-separated.
# Built-ins: scroll, reels, like, comment, share
# Extras (if added): follow, unfollow, save, openProfile, explore, dm, viewStories
ACTIONS=scroll,like,comment

# Default recipient for the "share" action (username)
SHARE_TO=

# Default comment text for the "comment" action
COMMENT_TEXT=🔥

# Log level: fatal | error | warn | info | debug | trace
LOG_LEVEL=info

# ─────────────────────────────────────────────────────────────────────────────
# Surface & Scrolling
# ─────────────────────────────────────────────────────────────────────────────

# SURFACE controls where we start AND where we stay:
#   home  = https://www.instagram.com/
#   reels = https://www.instagram.com/reels/
SURFACE=home

# Auto-scroll delay between scroll steps
# Use ONE of these:
# SCROLL_DELAY_SEC=1.5         # seconds (float ok)
# SCROLL_DELAY_MS=600          # milliseconds (integer)
SCROLL_DELAY_SEC=1.5

# How many pixels to scroll each step
SCROLL_STEP=1200

# ─────────────────────────────────────────────────────────────────────────────
# Optional: Task inputs (only if you use those tasks)
# ─────────────────────────────────────────────────────────────────────────────

# follow / unfollow modules (comma-separated usernames)
FOLLOW_USERS=
UNFOLLOW_USERS=

# openProfile module (comma-separated usernames to open)
OPEN_PROFILES=

# dm module recipients + default message
DM_TO=
DM_TEXT=👋

# ─────────────────────────────────────────────────────────────────────────────
# Development behavior
# ─────────────────────────────────────────────────────────────────────────────
# If auth fails:
# - In production (NODE_ENV≠development): the app exits immediately.
# - In development (NODE_ENV=development): it waits 60s before exiting to aid debugging.
# Set this in your start script or here if you prefer:
# NODE_ENV=development
